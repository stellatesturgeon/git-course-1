Команды для терминала

cd папка // переход в папку
mkdir папка // создание папки
pwd // показвает, где нахожусь в терминале
ls // показывает, что находится в папке
ls -la // отображение скрытых папок
echo "Some text" > file.txt // создает файл
cat название файла // чтение файла
rm название файла // удаление файла
clear // очищает терминал
------------------------------------------------
Команды для гита

git --version // версия гита
git add название файла // добавление в индекс измененного файла
git add . // добавление в индекс всех измененных файлов
git branch // показывает список всех веток, которые есть в локальном репозитории
git branch -a // показывает все ветки, в том числе, которые находятся в гитхабе
git branch -m имя ветки // сменить наименование ветки (выполняется только для ветки в которой находишься)
git branch -d имя ветки // удаление ветки (текущую ветку удалить нельзя)
git branch имя ветки // создание новой ветки
git cat-file -t 61e1886 // посмотреть тип коммита (после -t идет первые 7 символов из коммита)
git cat-file -p 61e1886 // посмотреть содерживое коммита
git clone url // скачивание репозитория с гитхаба себе на комп 
git checkout commit hash // переход к определенной версии
git checkout имя ветки // переход в определенную версию проекта по названию ветки (переключает на ветку)
git checkout -b имя ветки // создание новой ветки и переход в нее
git commit -m "что сделано, что добавляем" // сохраняет текущую версию проекта (-m - используется для того, чтобы сразу указать сообщение)
git config --global user.name "какое-то имя" // устанавливает имя автора
git config --global user.email mail // устанавливает почту автора
git config --list // получение всех настроек гита
git init // создание репозитория
git log // просмотр истории изменений (коммитов)
git merge -m "сообщение для комммита" название ветки, которую мы вливаем
git pull // скачивание изменений в коде из гитхаба
git push // отправка изменений в коде в гитхаб
git push -u origin ветка // загрузка изменений из локальной ветки в удаленную с созданием связи между ними (выполняется для каждой ветки отдельно, делается только первый раз и единожды)
git remote // показывает, есть ли в локальном репозитории удаленные сервера
git remote add origin url // подключение удаленного репозитория
git remove -v // проверяет настройку удаленного сервера (показывает название удаленного сервера и ссылку на репозиторий в гитхабе)
git status // показывает файлы, которые были изменены